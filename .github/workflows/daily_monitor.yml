name: Daily Momentum Monitor

on:
  schedule:
    # 21:30 UTC = 5:30 PM ET (summer) — ~90 min after US market close
    # Yahoo Finance EOD data is typically available within 30-60 min of close
    - cron: '30 21 * * 1-5'
  workflow_dispatch:   # allow manual trigger from the Actions tab

jobs:
  send-monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements_monitor.txt

      - name: Install dependencies
        run: pip install -r requirements_monitor.txt

      - name: Verify secrets are set
        env:
          GMAIL_USER:         ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
        run: |
          if [ -z "$GMAIL_USER" ];         then echo "ERROR: GMAIL_USER secret is missing or empty";         exit 1; fi
          if [ -z "$GMAIL_APP_PASSWORD" ]; then echo "ERROR: GMAIL_APP_PASSWORD secret is missing or empty"; exit 1; fi
          echo "OK: both secrets present (user=${GMAIL_USER:0:4}...)"

      - name: Run monitor and send email
        env:
          GMAIL_USER:         ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
        run: python monitor_gha.py
